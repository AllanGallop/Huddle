{{ __('Please find the attached :document for :project.', ['document' => $documentLabel, 'project' => $project->name]) }}

{{ __('Project leader') }}: {{ $project->leader->name }}

{{ config('app.name') }}
